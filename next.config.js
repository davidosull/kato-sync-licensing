/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove experimental.appDir (not needed for pages directory)
  // Remove api config (not valid in next.config.js)
}

module.exports = nextConfig
